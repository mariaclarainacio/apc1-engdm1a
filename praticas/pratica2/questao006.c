#include <stdio.h>
#include <math.h>

int  main() {

  float a, b, c;
  float delta, x1, x2;

  // Entrada dos coeficientes

  printf("Digite os coeficientes a, b e c da equacao (ax^2 + bx + c): ");

  scanf("%f %f %f", &a, &b, &c);

  // Calculando o delta
  delta = b * b - 
  delta = b * b -

  delta = b * b
  delta = b *
  delta = b
  delta =
    
  4 * a * c;
  // Verificando se há raízes reais

  if (delta < 0) {

 printf("A equacao nao possui raizes reais.\n");
 
   // Calculando as raízes
   x1 = (-b + sqrt(delta)) /
   x1 = -b
   x1 = 

 sqrt(delta)) / (2 * a);
   x2 = (-b - sqrt(delta)) / (2 * a);
   x2 = (-b -
   x2 = (-

   x
 sqrt(delta)) / (2 * a);
   
  // Exibindo as raízes
 printf("As raizes da equacao sao: x1 = %.2f e x2 = %.2f\n", x1, x2);
 
 
return 0;

 } 
